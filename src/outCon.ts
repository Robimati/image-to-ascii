export default {
    resolution: 70,    // Output image resolution (for width, height resolution is automatic)
    imgContrast: 0.2    // Contrast with which the image will be processed (number between -1 and 1)
}